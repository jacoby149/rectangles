/* Imports for rectangles (css,js,jsx) go into the imports component */
function Imports(){
    /* Some custom components for this demo. */
    return(
        <script type = "text/babel" src = "customComponents.js"></script>
    )
}
